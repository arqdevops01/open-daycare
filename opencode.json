{
  "$schema": "https://opencode.ai/config.json",
  "lsp": true,
  "mcp": {
    "playwright": {
      "type": "remote",
      "url": "http://localhost:8931/mcp",
      "enabled": true
    }
  },
  "references": {
    "db-schema": {
      "path": "../07-DB-Schema",
      "description": "Schema de base datos, tablas, columnas y referencias a crear, esto no está implementado en la base de datos, solo es para referencia"
    }
  }
}
